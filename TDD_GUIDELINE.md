# TDD 开发规范（AI 助手必读）

## ⚠️ 重要提醒

**每次修改代码前，必须先问："这个修改遵循 TDD 了吗？"**

## TDD 三步走（必须严格执行）

### 1. 🔴 红阶段 - 编写测试
- [ ] 先写测试，描述期望的行为
- [ ] 运行测试，确认它失败（红）
- [ ] 如果测试通过，说明测试写得有问题，或者功能已存在

### 2. 🟢 绿阶段 - 实现功能  
- [ ] 编写最简单的代码使测试通过
- [ ] 不要过度设计，先让测试变绿
- [ ] 运行测试，确认通过（绿）

### 3. 🔵 重构阶段 - 优化代码
- [ ] 在测试保护下重构代码
- [ ] 运行测试，确保仍然通过
- [ ] 提交代码

## 禁止行为 ❌

1. **直接修改功能代码而不写测试**
2. **先改代码再补测试**（这是作弊！）
3. **跳过红阶段**（不验证测试失败就改代码）

## 自检清单（每次提交前）

```
□ 我是否先写了测试？
□ 我是否看到测试失败（红）？
□ 我是否只写了让测试通过的最简单代码？
□ 所有测试是否都通过？
□ 我是否重构了代码（可选）？
```

## 常见借口与反驳

| 借口 | 反驳 |
|------|------|
| "这只是小修改，不需要测试" | 小修改最容易引入 bug |
| "测试已经存在了" | 那运行它，确保它失败 |
| "我先实现，再补测试" | 这是 TDD 的反面，禁止！ |
| "时间紧，没时间写测试" | 写测试是投资，不是开销 |

## 示例：正确的 TDD 流程

```typescript
// Step 1: 写测试（红）
it("应该计算两个数的和", () => {
  expect(add(1, 2)).toBe(3);  // 测试会失败，因为 add 不存在
});

// Step 2: 运行测试，确认失败
// npm test -- tests/add.test.ts
// ❌ FAIL: add is not defined

// Step 3: 实现最简单的代码（绿）
function add(a: number, b: number) {
  return a + b;
}

// Step 4: 运行测试，确认通过
// npm test -- tests/add.test.ts
// ✅ PASS

// Step 5: 重构（可选）
// 代码已经很简单，不需要重构
```

## 违规记录

| 日期 | 违规描述 | 教训 |
|------|----------|------|
| 2026-03-30 | 直接修改 /help 命令，后补测试 | 应该先写测试验证缺失的命令 |

---

**记住：TDD 不是可选项，是强制要求！**
